function buildPacking(){const text=document.querySelector('#packing-input').value.trim();const lines=text.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);const counts={};lines.forEach(x=>counts[x]=(counts[x]||0)+1);const rows=Object.entries(counts).map(([ean,q])=>({ean,q}));document.querySelector('#packing-result').innerHTML=rows.map(r=>`<tr><td>${r.ean}</td><td>${r.q}</td></tr>`).join('');document.querySelector('#packing-total').textContent=lines.length}document.addEventListener('DOMContentLoaded',()=>document.querySelector('#build-packing')?.addEventListener('click',buildPacking));
